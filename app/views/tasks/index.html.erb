<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><input type="checkbox" <%=checked(task)%> disabled><%= link_to(task.title, task_path(task))%></li>
  <% end %>
</ul>

<%= link_to  "Add a new Task", new_task_path %>
